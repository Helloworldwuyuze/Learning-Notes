#include<bits/stdc++.h>
#include<windows.h>

using namespace std;

int main(){
	do{
		system("start sj.exe");
		Sleep(100);
		system("start F.exe");
		Sleep(500);
	}while(!system("fc F.out answer.out"));
	return 0;
}
